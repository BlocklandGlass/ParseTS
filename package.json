{
  "name": "ParseTS",
  "version": "0.0.0",
  "description": "Node scripts: TorqueScript parser/linter written in Haskell. Not complete yet, but should ideally generate a complete AST for any valid TorqueScript program.",
  "dependencies": {},
  "devDependencies": {
    "browser-sync": "^2.8.2",
    "gulp-shell": "^0.4.2",
    "gulp": "^3.9.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/BlocklandGlass/ParseTS.git"
  },
  "author": "",
  "bugs": {
    "url": "https://github.com/BlocklandGlass/ParseTS/issues"
  }
}
